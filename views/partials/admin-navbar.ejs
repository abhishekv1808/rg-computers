<!-- Admin Navbar -->
<nav class="bg-white border-b border-gray-200 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-6">
        <div class="flex justify-between h-16">
            <!-- Logo & Nav Links -->
            <div class="flex items-center">
                <a href="/admin/dashboard" class="flex items-center mr-10 hover:opacity-80 transition-opacity">
                    <img src="/images/RgComputers-logo.png" alt="RG Computers" class="h-10 object-contain">
                </a>
                <div class="hidden md:flex space-x-1">
                    <a href="/admin/dashboard"
                        class="px-3 py-2 text-sm font-medium rounded-md transition-all duration-200 <%= path === '/admin/dashboard' ? 'bg-blue-50 text-blue-700' : 'text-gray-500 hover:text-gray-900 hover:bg-gray-50' %>">
                        <i class="fas fa-chart-pie mr-1.5 <%= path === '/admin/dashboard' ? 'text-blue-600' : 'text-gray-400' %>"></i>
                        Dashboard
                    </a>
                    <a href="/admin/inventory"
                        class="px-3 py-2 text-sm font-medium rounded-md transition-all duration-200 <%= path === '/admin/inventory' ? 'bg-blue-50 text-blue-700' : 'text-gray-500 hover:text-gray-900 hover:bg-gray-50' %>">
                         <i class="fas fa-box mr-1.5 <%= path === '/admin/inventory' ? 'text-blue-600' : 'text-gray-400' %>"></i>
                        Inventory
                    </a>
                    <a href="#"
                        class="px-3 py-2 text-sm font-medium rounded-md transition-all duration-200 text-gray-500 hover:text-gray-900 hover:bg-gray-50">
                        <i class="fas fa-shopping-bag mr-1.5 text-gray-400"></i>
                         Orders
                         <span class="ml-1.5 bg-red-100 text-red-600 px-1.5 py-0.5 rounded-full text-[10px] font-bold">New</span>
                    </a>
                    <a href="#"
                        class="px-3 py-2 text-sm font-medium rounded-md transition-all duration-200 text-gray-500 hover:text-gray-900 hover:bg-gray-50">
                        <i class="fas fa-users mr-1.5 text-gray-400"></i>
                        Customers
                    </a>
                </div>
            </div>

            <!-- Right Side Actions -->
            <div class="flex items-center space-x-4">
                <button class="relative text-gray-400 hover:text-gray-500 transition-colors">
                    <i class="far fa-bell text-lg"></i>
                    <span class="absolute top-0 right-0 block h-2 w-2 rounded-full ring-2 ring-white bg-red-500 transform translate-x-1/2 -translate-y-1/2"></span>
                </button>
                <div class="flex items-center gap-3 pl-4 border-l border-gray-200">
                    <div class="text-right hidden sm:block">
                        <p class="text-sm font-semibold text-gray-900">Admin User</p>
                        <p class="text-xs text-gray-500">Manager</p>
                    </div>
                    <div
                        class="h-9 w-9 bg-gradient-to-br from-blue-500 to-blue-600 rounded-full flex items-center justify-center text-white font-bold shadow-md shadow-blue-200">
                        A
                    </div>
                    <form action="/admin/logout" method="POST">
                        <button type="submit" class="text-gray-400 hover:text-red-500 ml-2 transition-colors p-1" title="Logout">
                            <i class="fas fa-sign-out-alt"></i>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</nav>
